target/

# Candidate wordlist / scratch text dumps (generated; never commit)
*.txt
# ...except source assets embedded via include_str! (must be tracked, or clones
# fail to build).
!src/logo.txt
.claude/

# Private, local-only engineering tracker (see issues/README.md). Working notes, not
# user documentation — never published with the crate or the repo.
issues/

# Local-only Claude Code project instructions. Working guidance, not shipped docs.
CLAUDE.md
